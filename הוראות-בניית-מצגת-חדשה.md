# מדריך בניית מצגת הלכה — המדריך המלא

מסמך זה הוא **המדריך המרכזי והיחיד** לבניית ולעריכת מצגת בנושא הלכתי בסדרה. הוא מרכז את השלד הטכני (תיקיות, קבצים, פלטה, ניווט, תמונות, עמוד בית), את מלוא עקרונות העריכה והבקרה (אימות מקורות, שפה, טון, מבנה, איזון הלכתי, חידון), ולקחים מתיקונים קודמים (נספח א). אין צורך לחזור על ההוראות בכל פעם — הכול כאן.

**סדר עבודה מומלץ למצגת חדשה:**
1. אימות מקורות ואיסוף התוכן (סעיף 7) — לפני הכול.
2. שלד הקובץ ומיקומו (סעיפים 1–2).
3. פלטת נושא (סעיף 3).
4. בניית השקפים והתוכן (סעיפים 4–6).
5. חידון (סעיף 10).
6. לוגיקת התמונות — סלוטים (סעיף 11).
7. רישום בעמוד הבית עם סימון "בפיתוח" (סעיף 12), והסרתו בסיום.
8. בקרת איכות ובטיחות טכנית (סעיפים 13–14).

לעבוד **פרק-פרק עם אישור המשתמש בכל שלב**, ולרכז כל מספור-מחדש לפעולה אחת בסוף. חלוקה לפרקים (מחיצות) ופיצול שקפים הם **החלטה של המשתמש, לא אוטומטית** — להציע ולהמתין לאישור.

---

## 1. מבנה התיקיות ומיקום הקובץ

```
index.html            ← עמוד הבית (מפת המצגות)
css/                  ← גיליונות סגנון משותפים לכל המצגות
js/                   ← סקריפטים משותפים לכל המצגות
shabbat/  kashrut/  tefila-brachot/  chagim/   ← תיקייה לכל נושא-על
<נושא>/images/<שם-מצגת>/ ← תת-תיקייה ייעודית לתמונות של כל מצגת
```

- **כל מצגת היא קובץ HTML בודד** בתוך תיקיית נושא-העל שלה. הנושא קובע את התיקייה: `shabbat/`, `kashrut/`, `tefila-brachot/`, `chagim/`. לנושא-על חדש — ליצור תיקייה חדשה בשורש.
- **שם הקובץ** באנגלית, kebab-case, מתאר את הנושא: `borer.html`, `basar-vehalav.html`, `min-hatzomeah.html`.
- מכיוון שהקובץ יושב תיקייה אחת מתחת לשורש, כל הקישורים לקבצים המשותפים הם יחסיים עם `../`: `../css/...`, `../js/...`.
- **תמונות המצגת** יושבות ב**תת-תיקייה ייעודית לכל מצגת** בתוך `images/` של הנושא, ושם הקובץ לפי התבנית האחידה — ראו סעיף 11. הנתיב בקוד: `./images/<שם-מצגת>/<שם-קובץ>` (למשל `./images/borer/borer-05-tnai-ochel.png`). התת-תיקייה נוצרת בשמירת התמונה הראשונה.

## 2. שלד הקובץ (HTML)

**ה-`<head>` — סדר קישורי ה-CSS קבוע:**

```html
<html dir="rtl" lang="he" class="rtl-mode" data-theme="shabbat">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>הלכות … — מצגת</title>
  <link rel="stylesheet" href="../css/variables.css" />   <!-- משתני צבע/נושא/מצב -->
  <link rel="stylesheet" href="../css/layout.css" />       <!-- מבנה: סיידבר, שקף, פס ניווט -->
  <link rel="stylesheet" href="../css/components.css" />   <!-- כל הרכיבים: תיבות, חידון, טבלאות, קרוסלה, סלוטים -->
  <link rel="stylesheet" href="../css/<topic>.css" />      <!-- אופציונלי: CSS ייעודי למצגת -->
  <link rel="stylesheet" href="../css/utilities-extracted.css" /> <!-- מחלקות עזר אטומיות (mt-20px, two-columns וכו') -->
</head>
```

- `data-theme` על תגית `<html>` הוא **הדבר היחיד שקובע את פלטת הצבעים** (סעיף 3).
- `class="rtl-mode"` מגדיר כיווניות RTL וכיווני חצים.
- `<topic>.css` הוא קובץ קטן ורשות לכללים ייחודיים למצגת (כמו `borer.css`, `kelim.css`). אם אין צורך — לדלג עליו.

**סדר תגי ה-`<script>` בתחתית ה-`<body>` (קבוע):**

```html
<script src="../js/navigation.js"></script>   <!-- ניווט, סיידבר, מקלדת -->
<script src="../js/theme.js"></script>         <!-- מצב בהיר/כהה -->
<script src="../js/quiz.js"></script>          <!-- מנוע חידון -->
<script src="../js/carousel.js"></script>      <!-- קרוסלות תמונה + Lightbox -->
<script src="../js/image-slots.js"></script>   <!-- סלוטי תמונה (חייב אחרי carousel.js) -->
<!-- אם המצגת כוללת תרשימי זרימה על canvas: -->
<script src="../js/flowcharts.js"></script>
```

`image-slots.js` **חייב** להיטען אחרי `carousel.js` (הוא משתמש ב-`window.initMelachaCarousels`). `flowcharts.js` נטען רק במצגות שיש בהן תרשימי זרימה.

## 3. פלטת צבעים לפי נושא

הצבעים מנוהלים ב-`css/variables.css` בשלוש שכבות: **נושא** (`[data-theme]`) → **מצב** בהיר/כהה (`[data-mode]`) → **משתנים חשופים** שהרכיבים צורכים (`--primary-color`, `--text-primary`, `--box-warning-bg` וכו'). מצגת בוחרת פלטה **אך ורק** דרך `data-theme` על `<html>` — כל השאר נגזר אוטומטית וגם עובד במצב כהה.

**נושאים קיימים:** `shabbat`, `kashrut`, `tefila`, `chagim`, וכן נושאים גנריים לפי צבע: `warm` (ברירת מחדל), `blue`, `gray`, `cyan`, `green`, `red`.

**לבחור פלטה קיימת** — פשוט `data-theme="kashrut"`.

**להוסיף פלטה חדשה לנושא-על חדש** — להוסיף בלוק ל-`variables.css`, המגדיר ארבעה משתני יסוד בלבד:

```css
[data-theme="<שם-הנושא>"] {
    --theme-primary:      #44618C;  /* צבע ראשי */
    --theme-primary-dark: #32496B;  /* גרסה כהה לכותרות */
    --theme-secondary:    #C29A3A;  /* צבע משני/מבטא */
    --theme-tertiary:     #4A7280;  /* צבע שלישי (תיבות מידע/היתר) */
}
```

כל התיבות, הכפתורים, הטבלאות והתרשימים נגזרים מארבעת אלה. **חובה:** בכל רכיב, טבלה או תרשים חדש להשתמש במשתני ה-CSS (`--primary-color`, `--bg-lightest-gray`, `--box-success-bg`, `--text-muted` …) ולא בצבעים קשיחים — כדי שישתלב בעיצוב ובמצב כהה/בהיר.

## 4. מבנה המצגת והשקפים

הגוף בנוי כך: כפתור פתיחת סיידבר (מובייל) → `<aside class="slides-sidebar">` (נבנה אוטומטית) → `<div class="slideshow-container">` ובתוכו השקפים → פס ניווט תחתון.

**שקף שער** (רק לו יש `active`):
```html
<div class="slide active title-slide">
  <h1>🕯️ הלכות … 🕯️</h1>
  <div class="subtitle"><p>תת-כותרת</p><p>על פי פניני הלכה, ילקוט יוסף, שולחן ערוך ומשנה ברורה</p></div>
</div>
```

**תוכן עניינים** — פריטים לחיצים המנווטים לשקף (N הוא אינדקס 0-based):
```html
<div class="slide">
  <h2>תוכן עניינים</h2>
  <div class="content">
    <span class="slide-link-span" onclick="showSlide(3)">מה זה בורר? (שקף 4)</span>
    …
  </div>
</div>
```

**שקף מחיצה (פרק)** — להחלטת המשתמש, לא אוטומטית:
```html
<div class="slide section-slide">
  <div class="section-slide-inner">
    <span class="section-kicker">חלק א</span>
    <h2>יסודות המלאכה</h2>
    <p class="section-sub">…</p>
  </div>
</div>
```

**שקף רגיל** — כותרת `h2`, מעטפת `content`, ובתוכה תיבות התוכן:
```html
<div class="slide">
  <h2>כותרת השקף</h2>
  <div class="content">
    <div class="source-box">ציטוט/רקע
      <div class="source-ref-box"><strong>מקור:</strong> פניני הלכה … | שו"ע …</div>
    </div>
    <div class="definition-box"><h3>הגדרה</h3><p>…</p></div>
    <div class="positive-box">…</div>   <!-- היתר/חיוב (ירוק) -->
    <div class="warning-box">…</div>    <!-- איסור/אזהרה -->
    <div class="example-box">…</div>    <!-- דוגמה/מידע -->
  </div>
</div>
```

מילון התיבות: `definition-box` (הגדרה), `positive-box` (היתר/חיוב), `warning-box` (איסור/אזהרה), `example-box` (דוגמה/מידע), `source-box` (ציטוט מקור) ובתוכה **תמיד** `source-ref-box` להפניה המדויקת. לשמר מגירות `<details>` כחומר לימוד עצמי מכוּון.

**שקף מקורות** בסוף, ו**פס ניווט תחתון**:
```html
<nav class="navigation-controls" aria-label="ניווט בין שקפים">
  <button class="nav-btn" onclick="previousSlide()" id="prev-btn">▶ קודם</button>
  <div class="slide-counter"><span id="current-slide">1</span> / <span id="total-slides">1</span></div>
  <button class="nav-btn" onclick="nextSlide()" id="next-btn">הבא ◀</button>
</nav>
```

הסיידבר נבנה אוטומטית מכל `.slide` (הכותרת נלקחת מ-`h2`/`h1`) — אין צורך לתחזק אותו ידנית.

## 5. ניווט, קישורים פנימיים ועקביות

- `showSlide(n)` — פונקציית הליבה, **0-based** (עם גלישה מודולרית). `goToSlide(N)` — **1-based** (לשימוש בחידון). `nextSlide()` / `previousSlide()` — עם גבול.
- מקלדת: חצים/רווח; מגע: סוויפ אופקי (RTL). הכול מובנה ב-`navigation.js`.
- **קישור פנימי / תוכן עניינים:** `<span class="slide-link-span" onclick="showSlide(N)">…</span>`.
- **שאלת חידון → שקף:** קישור עם `onclick="goToSlide(N)"` לשקף שבו נדון הנושא.
- **משמעת מספור:** אם פיצלת או הוספת שקף — למספר מחדש בכל מקום **בפעולה אחת**: תוכן עניינים, `showSlide/goToSlide`, "לפירוט בשקף N", והצלבות בפרוזה. לאחר הוספת שקף — לגשר עליו בתיבת-הפתיחה של השקף הבא ("מן ההיתר אל המעשה").
- **עקביות פנימית:** מספרים ומניין חייבים להתאים לפריטים בפועל — אם נכתב "שני סוגים", שלא יופיעו אחריו שלושה. לאחר כל שינוי תוכן — לבדוק שכל ההפניות אליו (חידון, תוכן עניינים, הצלבות בפרוזה) עדיין נכונות. מונחים, שמות ומספרי שקפים אחידים לאורך כל המצגת.
- **הצלבה בין-מצגתית:** בשם הסדרה המדויק, בניסוח «מצגת '…'». לא לשכפל בין מצגות שקפים ייחודיים לאחת מהן (למשל שקף "מבוא" כללי הקיים רק במצגת אחת).

## 6. בניית תוכן — עומק וייצוג חזותי

- **להסביר את הטעם, לא רק את הדין** — להקדים או לצרף את ההיגיון שמאחורי ההלכה, ולענות על השאלה המתבקשת מאליה.
- **להרבות בדוגמאות קונקרטיות מהמטבח/השולחן/הסופר.** כל כלל מופשט — ללוות בדוגמה מוחשית ומיידית. בגבול דק בין מותר לאסור — להציג **זוג דוגמאות מנוגדות** שמחדד היכן עובר הקו. להעדיף «דוגמה:» קצרה על הסבר תאורטי ארוך.
- **להבחין בין זמן הבית לימינו** היכן שהדין השתנה — מה עשו כשבית המקדש קיים ומה נוהגים היום ומדוע.
- **לשקף את ההלכה במלואה** — רוב ומיעוט, קולות וחומרות, בלי להחמיר מעבר לדין (למשל שאין חובה בשקית גניזה מיוחדת, ושיש הקוברים בקרקע).
- **בהירות:** מונח טכני שמופיע לראשונה — להגדירו בקצרה במקום ("דמאי — יבול שמעמדו מסופק"). לנסח כמויות כך שלא ישתמעו לשתי פנים ("יותר מאחוז"→"מעט יותר מאחוז").
- **עיקרון-על חזותי — להרבות בייצוג חזותי, לא להחסיר.** כל תהליך → תרשים זרימה/החלטה (צומת-אחר-צומת, וגם רצף "אם… אז…"); כל חלוקה כמותית (אחוזים, שיעורי המתנה) → מלבנים/עוגה; כל השוואה רב-ממדית (עדה/שיעור/טעם/מקור/מותר-אסור) → **טבלה** (`.halacha-table`, RTL, כותרות מודגשות, שורות מתחלפות). עדיף ייצוג חזותי קומפקטי על פסקה צפופה. תרשימים פשוטים ב-HTML/CSS (תיבות וחיצים); SVG רק כשנדרשת גרפיקה מדויקת; `flowcharts.js` לתרשים canvas.
- תוספת תוכן — מוטב **להוסיף לשקף קיים** מאשר לפתוח שקף חדש (מונע מספור-מחדש שביר).

## 7. אימות מקורות — הקריטי ביותר ⚠️

זהו הלב. כל תיקון כאן הוא תיקון של **אמת**, וקודם לכל השאר. המקורות הראשיים של הסדרה: **פניני הלכה** (הרב מלמד), **ילקוט יוסף** (הרב עובדיה יוסף), **שולחן ערוך** ו**משנה ברורה**.

- **כל ציטוט בלשון המקור** (גמרא, שו"ע, רמב"ם, מדרש) — לאמת מילה במילה מול המקור לפני שמסתמכים עליו. תשומת לב מיוחדת לארמית ולשיוך האומר הנכון (מי אמר את המימרה).
- **כל פסוק** — לאמת מין, נטייה וניקוד; פסוקים שהולחמו או קוצרו — לתייג «(מקוצר)».
- **כל מספר סימן / סעיף / ס"ק** — לאמת, לא להניח מספור.
- ציטוט שהוא בעצם פרפראזה רופפת — לנסחו כפרפראזה מפורשת, לא בתוך מירכאות.
- **מקור שלא ניתן לאמת — לסמן למשתמש לבדיקה. לא להמציא, ולא למחוק בשתיקה.**
- לפני כל הסבר הלכתי מהותי — לאמת את הטעם מול מקור, לא מהזיכרון.
- **אימות עובדות חוץ-הלכתיות** — תאריכים, אחוזי נגיעות, נתונים על גופים/ארגונים: לאמת מול מקור עדכני, לא להניח.
- **הערה מתודית:** אתר ספריא טוען טקסט ב-JavaScript, ולכן `WebFetch` מחזיר ממנו מטא-דאטה בלבד ולא ניתן לאמת דרכו מילה-במילה סעיפי שו"ע. לעגן את האימות ב**פניני הלכה** (ph.yhb.org.il) וב**ילקוט יוסף** (טקסט נגיש), ולסמן למשתמש כל סעיף שו"ע/גמרא שלא אומת מילולית — במקום להניח.

## 8. שפה, כתיב וטון

- **כתיב מלא וחסר עקבי** לפי כללי האקדמיה, יו"ד כפולה היכן שצריך: «ציווים»→«ציוויים», «ויחוד»→«וייחוד».
- **דקדוק מדויק** — התאמת מין ומספר בכל כינוי ונטייה: «אין בהן»(מעשרות, זכר)→«אין בהם»; «סוגיהן»→«סוגיהם».
- **לפרק מבני סמיכות כבדים** «X של Y» / «X עם Y» → «סיר המכיל מרק».
- מקף בלועזיות: «קוקה קולה»→«קוקה-קולה». להחליף לעז במילה עברית: «אינוונטר»→«אוצר». סדר מילים לפי צליל: «בנפש ובלב»→«בלב ובנפש».
- **טון:** לפתוח שקף בוו/רעיון אנושי ולא בכותרת-מילון יבשה ("נתחיל מעובדה פשוטה…"); קול חם, אישי, זורם; משפטים קצרים; גוף ראשון-רבים ("אנו שומרים"). להחליף ניסוח מרוחק בקרוב: «שאדם מישראל»→«שיהודי», «מקפיד מה»→«שם לב למה». חום וזרימה **לעולם לא על חשבון דיוק הלכתי**.

## 9. איזון ומסגור הלכתי

- לא להציג דעת מיעוט מקילה כאילו היא הכלל; להציג רוב ומיעוט, ומסגרת אזהרה כשרוב הפוסקים מחמירים ו«מנהג ישראל תורה» (כך בעניין דג ובשר).
- לא להציג אופציה אחת כיחידה כשיש שונות לפי מנהג/עדה (למשל «שש שעות» כברירת מחדל אחת).
- נימה ניטרלית — להסיר מילות שיפוט («משעשע» וכדומה) ולהשאיר את ההכרעה לצופה.

## 10. חידון

- שאלות כלליות-פרקטיות ממצבי יום-יום ("היכן נעשית השחיטה?"), לא ציטוט-שקף ולא תרחישים ארוכים ומפותלים.
- ארבע אפשרויות באורך דומה ומאוזן. **התשובה הנכונה לא תהיה הארוכה או המפורטת ביותר** — **מבחן מעשי:** אם אסטרטגיית "סמן בכל שאלה את האפשרות הארוכה ביותר" עוברת את החידון, החידון נכשל וטעון איזון. אם התשובה הנכונה ארוכה בהכרח — לדייק את השאלה או לפצלה.
- **המסיחים דומים באורך ובפירוט לתשובה הנכונה** (שגויים לגופם, כך שלא ניתן לנחש לפי צורה), אך **ברורים-שגויים** ואינם מתחרים בתשובה; להימנע ממסיח נכון-למחצה ("תלוי במנהג העדה" כשזה אכן נכון).
- **לפזר את אות התשובה הנכונה** על פני א/ב/ג/ד (למשל ~7 לכל אות בחידון של ~28 שאלות).
- לכל שאלה: **הסבר + מקור + קישור לשקף** (`goToSlide(N)`).
- היקף: עד 25–30 שאלות. שינית תוכן בשקף — עדכן את השאלה התלויה בו.

## 11. לוגיקת התמונות (מנגנון image-slots)

כל תמונה מתוכננת מיוצגת ב-HTML כ**סלוט** שהסקריפט `image-slots.js` מעבד: אם קובץ התמונה קיים — היא מוצגת; אם חסר — מוצגת קופסת "מקום שמור" עם תיאור ושם הקובץ, אך ורק כאשר ה-URL כולל `?showPlaceholders=true`; חסר וללא הפרמטר — לא מוצג דבר (שקוף למשתמש הרגיל).

**שלוש תבניות:**

תמונה בודדת:
```html
<div class="img-slot"
     data-src="./images/<שם-מצגת>/<שם-קובץ>.png"
     data-caption="כיתוב"
     data-note="תיאור התמונה לייצור"></div>
```

השוואת מותר/אסור (יחס 4:3):
```html
<div class="img-slot img-slot--compare">
  <div class="img-part" data-verdict="forbidden" data-src="./images/<מצגת>/a.png"
       data-tag="אסור: …" data-note="…"></div>
  <div class="img-part" data-verdict="allowed" data-src="./images/<מצגת>/b.png"
       data-tag="מותר: …" data-note="…"></div>
</div>
```

קרוסלה (יחס ריבועי):
```html
<div class="img-slot img-slot--carousel" data-figcaption="<strong>כותרת</strong>">
  <div class="img-part" data-src="./images/<מצגת>/x1.png" data-caption="שלב א" data-note="…"></div>
  <div class="img-part" data-src="./images/<מצגת>/x2.png" data-caption="שלב ב" data-note="…"></div>
</div>
```

**כללי מיקום ותוכן:**
- **מיקום בשקף:** איור "hero"/סקירה — בראש השקף. איור שממחיש דוגמאות טקסטואליות מסוימות (כמו "דינים מיוחדים") — **אחרי** הטקסט שאותו הוא ממחיש, כגלריית סיכום, לא לפניו.
- **תבנית שם הקובץ (אחידה):** `<שם-מצגת>-<מספר-שקף>-<נושא>.png` — למשל `borer-05-tnai-ochel.png`. מספר השקף הוא **מונה המצגת** (N/total, הכולל שער/תוכן/מחיצות; לצ׳ק-ליסט ללא שקפים כמו פסח משתמשים ברצף `s01`, `s02`…). כל תמונות המצגת יושבות ב**תת-תיקייה ייעודית** `./images/<שם-מצגת>/`, וב-`data-src` נכתב הנתיב המלא.
  *הערה:* מספר שקף בשם הקובץ שביר בעת מספור-מחדש — אם משנים סדר שקפים, יש לחדש את שמות הקבצים (חישוב מחדש של מספרי השקף ועדכון כל ה-URLים; ניתן לעשות זאת בסקריפט).
- הגדלים מוגבלים ב-CSS כדי שהכול ייכנס בשקף בלי גלילה: קרוסלה עד 380px, השוואה עד 760px, תמונה בודדת עד 360px גובה.
- **סגנון האיורים:** שטוח ונקי (flat), רקע שמנת, קו נקי, דמויות צנועות ואנונימיות, **ללא טקסט עברי בתוך התמונה** (הכיתוב ב-HTML). רגישות: אין תיאור ה', רבנים מזוהים או עבודה זרה; בשחיטה/מליחה ללא דם.

**זרימת העבודה:** פותחים את המצגת עם `?showPlaceholders=true` → רואים לכל תמונה חסרה שם קובץ מלא (עם תת-התיקייה) ותיאור → מייצרים ושומרים בשם המדויק ב-`./images/<שם-מצגת>/` → התמונה מופיעה אוטומטית.

## 12. הוספה לעמוד הבית + סימון "בפיתוח"

עמוד הבית `index.html` שבשורש הוא מפת המצגות. הוא מקושר ל-`css/variables.css` ו-`css/index.css` בלבד. כל נושא-על הוא `<section class="topic">` עם כותרת ומניין, ובתוכו רשת כרטיסים (`.cards`), וכל מצגת היא כרטיס-קישור.

**כרטיס פעיל (מצגת מוכנה):**
```html
<a class="card" href="<topic>/<file>.html">
  <div class="num">מצגת</div>
  <h3>הלכות …</h3>
  <p>תיאור קצר של תוכן המצגת.</p>
</a>
```

**סימון "בפיתוח" — בזמן העבודה על מצגת חדשה:** להוסיף את הכרטיס עם המחלקה `soon` (מעומעם, ללא ריחוף) ותג `badge-soon` עם אייקון כלי-עבודה. כך המצגת נראית בעמוד הבית אך מסומנת בבירור כלא-גמורה:
```html
<a class="card soon" href="<topic>/<file>.html">
  <div class="num">מצגת</div>
  <h3>הלכות … <span class="badge-soon">🛠️ בפיתוח</span></h3>
  <p>תיאור קצר.</p>
</a>
```
המחלקות `.card.soon` ו-`.badge-soon` כבר מוגדרות ב-`css/index.css`.

**בסיום הפיתוח:** להסיר את המחלקה `soon` ואת ה-`<span class="badge-soon">`, ולעדכן את מניין המצגות בכותרת הנושא (`<span class="count">(… מצגות)</span>`). אם זה נושא-על חדש — להוסיף `<section class="topic">` שלם.

## 13. בטיחות טכנית בעריכת הקבצים

הקבצים גדולים (~150–300KB) ומסונכרנים, ובאג **קיטוע-הזנב** חזר שוב ושוב: סוף הקובץ (שקף מקורות + פס ניווט + תגי `<script>` + `</html>`) נעלם לאחר כתיבות גדולות.

- להעדיף עריכות `Edit` ממוקדות. הוספה + מספור-מחדש נרחב — בסקריפט פייתון אחד עם `assert` לכל החלפה וכתיבה אטומית אחת (כולל `flush`+`fsync`+קריאת-בייטים חוזרת).
- ה-mount של ה-bash מפגר אחרי כלי הקובץ — **לא להריץ סקריפט bash שקורא-מתקן-כותב על הקובץ מיד אחרי עריכות `Edit`** (הוא עלול לקרוא עותק קטוע ולהפוך את הקיטוע לקנוני).
- **המקור הקובע הוא כלי ה-Read.** לאמת תמיד שהקובץ נחתם ב-`</html>` + כל תגי ה-`<script>` + תגיות `div`/`details` מאוזנות.

## 14. צ׳ק-ליסט השקה למצגת חדשה

- [ ] כל המקורות אומתו; מקורות שלא אומתו סומנו למשתמש (סעיף 7).
- [ ] שלד הקובץ תקין: `data-theme`, חמשת קישורי CSS, ותגי הסקריפט בסוף לפי הסדר (סעיף 2).
- [ ] פלטת נושא נבחרה/הוגדרה; כל רכיב חדש משתמש במשתני CSS (סעיף 3).
- [ ] מבנה: שער, תוכן עניינים לחיץ, מחיצות (אם אושרו), שקפי תוכן, שקף מקורות, פס ניווט (סעיף 4).
- [ ] מספור עקבי בכל ההפניות, ומספרים מתאימים לפריטים בפועל (סעיף 5).
- [ ] שפה, כתיב וטון עברו בקרה (סעיף 8); איזון הלכתי נשמר (סעיף 9).
- [ ] חידון מאוזן (מבחן "האפשרות הארוכה"), אות נכונה מפוזרת, הסבר+מקור+קישור לכל שאלה (סעיף 10).
- [ ] סלוטי תמונה הוטמעו בתבנית השמות; נבדק עם `?showPlaceholders=true` (סעיף 11).
- [ ] הקובץ נחתם תקין (`</html>` + סקריפטים + תגיות מאוזנות) (סעיף 13).
- [ ] נרשם בעמוד הבית; בזמן הפיתוח עם `soon` + `🛠️ בפיתוח`, ובסיום הוסר הסימון ועודכן המניין (סעיף 12).

---

## נספח א — לקחים מתיקונים קודמים (זיכרון סדרתי)

תיקונים ממשיים שנעשו בסדרה, כתמרורי אזהרה למצגות הבאות:

**דיוק לשון המקור ושיוך אומר:** «חקתי»→«חקקתי» (רמב"ם); «מיניהו קא גדלי»→«מיניה גבלי» (חולין סז ע"ב); חמש הלכות שחיטה הן **רב יהודה אמר שמואל** (לא "אמר רב"); בדיקת הסכין «אבישרא ואטופרא ואתלתא רוחתא» היא **משמיה דרבא** (לא "רב פפא").

**מין ונטייה בפסוקים:** «ויעל…ויכס»→«וַתַּעַל הַשְּׂלָו וַתְּכַס» (שמות טז,יג).

**מספרי סימנים:** רמב"ם מאכלות אסורות «כב»→«כא»; בפניני הלכה כשרות — **טבילת כלים = פרק לא**, כללי הכשרה = לב, הכשרת הכלים והמטבח = לג (אל תניח מספור).

**חומרים מודרניים — לא לדמות אוטומטית לחרס:** קרמיקה מוצקה וסכין קרמי (תחמוצת זירקוניום) דינם **כזכוכית** (הכשרה בהגעלה), לא כחרס שאין לו תקנה.

**"כבולעו כך פולטו" — גבול ההכשרה:** חום יבש (אפייה/צלייה בלא נוזל) בלע באוּר וטעון **ליבון**; מה שאינו עומד בליבון אינו ניתן להכשרה — **תבנית זכוכית/פיירקס לאפייה יבשה, וטפלון — אין להכשיר**. תמיד להבחין בין בליעה בנוזל (הגעלה) לחום יבש (ליבון).

**חידון:** מבחן "האפשרות הארוכה" נכשל במצגת "מאכלי נכרים" (08.2026) ותוקן — כל האפשרויות אוזנו והאות פוזרה; מסיח נכון-למחצה ("תלוי במנהג העדה") הוחלף במצגת הכלים.

**עובדות חוץ-הלכתיות:** תאריך הכשר חגבים תוקן (תש"פ→תשפ"ב); פרט על הרכב מוזיקלי תוקן (רביעייה, לא צמד). כל נתון מציאותי — לאמת.

---

*מסמך זה הוא המדריך המלא והיחיד לבניית מצגת. משלימים אותו לעניין התמונות בלבד: `תוכנית-תמונות-מצגות.md` (רשימת התמונות שקף-אחר-שקף) ו-`פרומפטים-לתמונות-מצגות.md` (פרומפטים לייצור).*
